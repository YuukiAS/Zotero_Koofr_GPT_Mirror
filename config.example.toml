[mirror]
output_dir = "C:/ZoteroGPTMirror"

[export]
source = "fixture"
copy_pdf = true
write_metadata = true
write_index = true

[zotero]
local_api = "http://127.0.0.1:23119/api/"

# Future Google Drive/rclone settings belong here once that phase is implemented.
# Do not put OAuth tokens, Koofr passwords, or Zotero API keys in this file.
